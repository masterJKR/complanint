<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.complanint.repository.UserRepo">

    <insert id="save" parameterType="com.complanint.Entity.User">
        insert into user2( userId, name, email, password, tel, role)
          values(#{userId}, #{name}, #{email}, #{password}, #{tel},
        #{role})
    </insert>

</mapper>